stages:
  preprocess:
    cmd: python src/preprocess.py
    deps:
      - data/raw
    outs:
      - data/processed
  train:
    cmd: python src/train.py --params params.yaml
    deps:
      - data/processed
      - src/train.py
      - params.yaml
    outs:
      - mlruns
      - models